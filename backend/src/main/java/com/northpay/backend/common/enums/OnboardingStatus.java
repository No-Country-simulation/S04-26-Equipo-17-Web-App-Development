package com.northpay.backend.common.enums;

public enum OnboardingStatus {
    INVITED,
    IN_PROGRESS,
    DOCUMENTS_UPLOADED,
    CONTRACT_SIGNED,
    PAYMENT_CONFIGURED,
    PENDING_VERIFICATION,
    CORRECTION_REQUIRED,
    ACTIVATED,
    REJECTED
}